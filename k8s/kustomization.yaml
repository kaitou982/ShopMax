apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap.yaml
  - mysql-deployment.yaml
  - redis-deployment.yaml
  - nacos-deployment.yaml
  - gateway-deployment.yaml
  - admin-ui-deployment.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/part-of: shopmax
