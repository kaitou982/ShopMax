# 开发环境配置
server:
  port: 8087

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shopmax?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: aptx4869

  cloud:
    nacos:
      discovery:
        enabled: true
      config:
        enabled: true

logging:
  level:
    root: info
    com.shop.community: debug
